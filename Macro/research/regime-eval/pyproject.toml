[project]
name = "macro-regime-eval"
version = "0.1.0"
description = "Isolated research lab for Macro Regime challenger evaluation"
requires-python = ">=3.12"
dependencies = []

[tool.unittest]
start-directory = "tests"

